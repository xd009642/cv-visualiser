syntax = "proto3";

package cv.visualiser.protocol;

import "v1/common.proto";

service Watcher {
    rpc Watch(WatchRequest) returns (stream Event) {}
}

message WatchRequest {
}

message Event {
    oneof event {
        common.Span new_span = 1;
    }
}
